#ifdef wire_hpp
#define wire_hpp

#include <vector>

#endif

enum state {
    EMPTY,
    ELECTRON_HEAD,
    ELECTRON_TAIL,
    CONDUCTOR,
};

class wire {

private:
    //std::vector<std::vector<bool>> updateGrid;
    


};